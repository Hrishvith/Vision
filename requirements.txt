# Core vision
opencv-python>=4.8.0
numpy>=1.24.0

# AI detection — highly recommended (best accuracy, full-body person detection)
# Install separately; uncomment the line below or run: pip install ultralytics
# ultralytics>=8.2.0

# HTTP client (camera → backend API calls)
requests>=2.31.0

# FastAPI backend
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# ML demand-prediction model
scikit-learn>=1.3.0
pandas>=2.0.0
joblib>=1.3.0
